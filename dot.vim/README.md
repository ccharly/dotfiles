My .vim
=======

It seems that installing python using homebrek breaks YCM. If you have any
trouble with that, you should consider the following command:
```
brew unlink python
```
PS: Maybe it's due to my current configuration

Also, when re-linking your python using homebrew, it sometimes cause crashes
when opening `vim`. Again, the command above should do the trick!
